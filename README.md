# claude-plugin-installer
One-script installer for Claude Code plugins &amp; MCP servers. Auto-detects Node/Python, registers MCP config, links skills globally, and generates token-efficient CLAUDE.md per plugin.
